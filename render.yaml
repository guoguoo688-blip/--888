services:
  - type: web
    name: rise888-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python api/market_server.py
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
